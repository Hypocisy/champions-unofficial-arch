# 旧版 -> Arch 重构版 功能缺口对比

对比对象:
- 旧版: `E:\dev\Champions`
- 新版(本项目): `E:\dev\champions-unofficial-arch`

## 完全缺失的功能

1. **Gateways to Eternity 兼容层**
   旧版 `common/integration/gateways_to_eternity/*`(`Gateways`、`GatewaysSetting`、`GatewaysSettingLoader`、`GatewaysToEternityCompat`)及配套 datagen `GatewaysConfigProvider` 在新版中完全不存在,build.gradle 里的 `gateways-to-eternity` 依赖也已移除。该功能是 NeoForge-only 的可选兼容,若需保留需重新实现。

2. **自定义统计项**
   旧版 `ModStats.java` 注册的 `champion_mobs_killed` 自定义 statistic,新版全局搜索 `CUSTOM_STAT` 无任何结果,已彻底丢失。

3. **进度成就(Advancement)**
   旧版 `ModAdvancementProvider` 生成的"击杀 champion"挑战成就,新版没有任何 advancement datagen provider,也没有对应的 JSON 资源。

4. **中键拾取直接生成完整词条蛋**
   旧版 `common/mixin/MinecraftMixin.java`(客户端,patch `Minecraft.pickBlock`)让创造模式中键拾取怪物时,直接生成一个配好当前词条的 champion egg。新版没有对应 mixin,也没有转移到其他事件桥接实现,功能直接丢失。

5. **麻痹效果的客户端输入抑制(行为弱化)**
   旧版 `ClientAffixEventsHandler` 监听 `MovementInputUpdateEvent`,麻痹时强制清空玩家的 shift/jump/前后左右移动输入。新版改为服务端每 tick 将速度的 X/Z 分量清零(`ParalysisEffect.applyEffectTick`),但 **Y 轴速度未清零**,被麻痹的玩家理论上仍可跳跃逃脱。此外新版 `ParalysisEffect` 类的文档注释声称会附加 `MOVEMENT_SLOWDOWN` + `WEAKNESS`,但实际代码中并未实现,注释与实现不符,需要修正。

6. **Fabric 端的发射器(dispenser)行为缺失**
   Champion egg 的发射器逻辑在 NeoForge 侧有注册(`ChampionEggDispenseBehavior`,在 `ChampionsNeoForge.java` 中调用),但 Fabric 模块下搜索 `Dispense`/`DispenserBlock` 无任何结果。共享逻辑 `ChampionEggItem.dispense()` 本身是平台无关的,Fabric 只是没有接线注册。

7. **伤害类型标签缺失**
   `enkindling_bullet` 伤害类型缺少旧版 `ModDamageTypeTagsProvider` 生成的 `DamageTypeTags.IS_FIRE` 归类。新版 `DamageTypeProvider` 只生成了 damage_type 本体 JSON,没有配套的 tags provider,可能影响免疫火焰/防火抗性相关判定。

8. **客户端 HUD 偏移配置**
   旧版 `ClientChampionsConfig`(`hudXOffset`/`hudYOffset`/`hudRange`/Jade 星星间距与底部内边距等)在新版中无任何等价物。新版 `HUDHelper` 的血条位置是硬编码值,无法自定义。

9. **全局战利品来源开关**
   旧版 `ConfigEnums.LootSource`(CONFIG / LOOT_TABLE / CONFIG_AND_LOOT_TABLE 三选一)在新版中没有等价配置项。新版 `ChampionLootHandler` 无条件同时 roll 战利品表掉落和配置字符串掉落,无法只启用其中一种来源。

10. **按实体的词条白名单/黑名单精细控制**
    旧版 `EntitiesConfig`(每个实体单独配置 minTier/maxTier/presetAffixes/affixList,以及词条允许/禁止列表)在新版中没有直接等价物。新版 `entity_filter` 只能做类型/标签/mob_category 级别的粗粒度过滤,无法像旧版那样精确控制某个具体实体能获得哪些词条。

11. **软依赖检测方法**
    `Utils.isGameStagesLoaded` / `isGatewaysLoaded` / `isScalingHealthLoaded` 这几个基于 `ModList.isLoaded` 的软依赖检测方法在新版中全部消失(其中 Gateways 相关功能本身也已整体缺失,属于配套移除)。

## 半成品:代码已写但未接通(等同功能缺失)

12. **KubeJS 自定义 Affix 定义**
    `neoforge/.../kubejs/ScriptableAffixType.java` 的实现与旧版 `CustomAffix`/`AffixBehavior`/`AffixCallbacks` 在功能设计上基本对等(覆盖 spawn/tick/attack/hurt/damage/heal/death 各类回调)。但 `ChampionsKubeJSPlugin.registerBuilderTypes` 中真正把该 builder 注册进 KubeJS `affix_type` 注册表的 `registry.of(...)` 调用被注释掉了,脚本作者实际上无法使用该功能。

13. **KubeJS 生成事件 JS 钩子未接通**
    `ChampionsJsEvents.SPAWN`/`PHASE` 事件定义齐全,并通过 `ChampionsJsEvents.register()` 方法绑定到内部事件总线的逻辑也写好了,但全项目搜索未发现任何调用 `register()` 的地方,该方法从未被执行,JS 端永远收不到生成事件。

14. **Fabric 的 KubeJS 支持整体缺失**
    旧版 KubeJS 集成是 loader-agnostic 的(位于 `common/`),新版 Fabric 模块下没有任何 KubeJS 集成包,`fabric/build.gradle` 中 KubeJS Fabric 依赖被注释掉,`fabric.mod.json` 里仅保留一个空的可选依赖声明。

## 架构性替代(功能保留,实现方式变化,非缺口)

以下功能在新版中通过不同机制实现,效果基本等价,不视为回归:

- **全局战利品修改器**:旧版基于 NeoForge `IGlobalLootModifier` 注册表 + datagen,新版改为 `ChampionLootHandler` 在死亡处理路径中直接程序化处理战利品表和配置掉落,绕开了 GLM 系统本身。
- **Datapack 内置注册表 provider**:旧版通过 `DatapackBuiltinEntriesProvider` 机制注册 `damage_type`,新版 `DamageTypeProvider` 直接生成同样的静态 JSON,生成机制不同但产物一致。
- **`RanksConfig`/`EntitiesConfig` 的大部分职责**:转移到 archetype JSON 数据包(`tier_range`/`affix_pools`/`entity_filter` 等),这是重构本身的既定方向,细粒度上有取舍(见第 10 条)但整体思路是数据驱动化。
- **信标附近检测**:旧版维护一个 tracked `BEACON_POS` set,新版改为按需扫描 chunk(`ChampionSpawnHandler.isNearActiveBeacon()`),配置字段名 `beaconProtectionRange` 保持一致,逻辑等价甚至更正确(避免了阻塞式判定)。

## 优先级建议

- **高优先级(玩家能直接感知到的行为差异)**:第 10 条(实体词条精细控制)、第 6 条(Fabric 发射器)、第 5 条(麻痹客户端输入抑制,尤其是 Y 轴速度未清零的漏洞)。
- **低成本高收益**:第 12、13 条只需取消注释 + 补一行调用即可修复,性价比很高。
- **低优先级(小众功能,可评估是否需要保留)**:Gateways 兼容层、自定义统计项、进度成就。
